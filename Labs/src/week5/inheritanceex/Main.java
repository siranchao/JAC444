package week5.inheritanceex;

public class Main {
//SuperClass -- which is your parent class
//SubClass -- which is your child class
	//class A : public class B   ---- class A extends class B
	///Private is only visible inside its own class or inner classes
	//public is available everywhere to everyone even outside the project
	//default is available in the same package, to sub classes, but not 
	//available outside the package
	//protected available outside the package to sub classes as well
	public static void main(String[] args) {
		
	}
}
